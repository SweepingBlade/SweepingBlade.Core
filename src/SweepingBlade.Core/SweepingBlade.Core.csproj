<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>netstandard2.0</TargetFramework>
        <LangVersion>latest</LangVersion>
        <RootNamespace>SweepingBlade</RootNamespace>
    </PropertyGroup>

    <PropertyGroup Condition="$(Configuration) == 'Release'" Label="NuGet">
        <PackageId>SweepingBlade.Core</PackageId>
        <PackageVersion>1.0.0</PackageVersion>
        <Authors>SweepingBlade</Authors>
        <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <PackageProjectUrl>https://github.com/SweepingBlade/SweepingBlade.Core</PackageProjectUrl>
        <Description>SweepingBlade is a software library that provides a fundamental structure to support the development of .NET applications.</Description>
        <PackageTags>Application Framework</PackageTags>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    </PropertyGroup>

</Project>
